{
  "name": "@raulrod/playground",
  "version": "0.0.0",
  "private": true,
  "description": "Consumer app that validates the public API of RaulRod UI (RRU-110).",
  "scripts": {
    "lint": "eslint . --no-error-on-unmatched-pattern"
  }
}
