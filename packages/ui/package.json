{
  "name": "@raulrod/ui",
  "version": "0.0.0",
  "private": true,
  "description": "React components for RaulRod UI (public entry point).",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "node scripts/check-utils.mjs && node scripts/check-layout.mjs && node scripts/check-text.mjs && node scripts/check-visually-hidden.mjs && node scripts/check-portal.mjs && node scripts/check-pattern.mjs"
  },
  "devDependencies": {
    "@raulrod/tokens": "workspace:*",
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0",
    "react": "^19.3.0",
    "react-dom": "^19.3.0",
    "typescript": "^7.0.2"
  }
}
